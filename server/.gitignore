bin/
obj/
publish/
logs/
*.user
.vs/
# Статика Mini App копируется из web/out при сборке — не источник.
src/CompDesignBot/wwwroot/
# Локальные переменные для scripts/run-dev.ps1 — секреты, не в git.
.env*
!.env.dev.example
