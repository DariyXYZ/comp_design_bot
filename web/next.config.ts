import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Статического экспорта больше нет: личному кабинету нужны API-роуты
  // (проверка подписи запуска, ключ Pyrus), а `output: 'export'` с ними
  // несовместим — сборка падает на первом же динамическом роуте. Приложение
  // живёт на Vercel, черновики — в его preview-деплоях.
  // На Vercel приложение живёт в корне домена, поэтому basePath не нужен.
  // Переменная оставлена для случая, если приложение снова будут раздавать из
  // подпапки (project pages).
  basePath: process.env.NEXT_PUBLIC_BASE_PATH || undefined,
  trailingSlash: true,
  // Оптимизатор картинок отключён сознательно: своих растровых картинок в
  // приложении нет — карточки тем приходят из Supabase Storage, а места без
  // изображений заняты знаком скрипта (inline SVG).
  images: { unoptimized: true },
  // Плашка Next.js в углу мешает смотреть черновик экранов: она закрывает
  // правый верхний угол ровно там, где у Mini App заголовок. Ошибки сборки и
  // рантайма Next всё равно покажет, отключается только индикатор.
  devIndicators: false,
};

export default nextConfig;
