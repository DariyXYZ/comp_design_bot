import { FeedScreen } from "./_components/feed-screen";

/**
 * Поток отдела — доска Pyrus в виде ленты. Данные живые, поэтому экран
 * клиентский: см. `_components/feed-screen`.
 */
export default function FeedPage() {
  return <FeedScreen />;
}
