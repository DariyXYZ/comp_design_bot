/**
 * Общие куски текста, которые должны звучать одинаково на всех экранах.
 *
 * Пока здесь одна фраза, и она заслуживает отдельного места: её приписывают к
 * разным сообщениям об ошибке, и если писать её каждый раз заново, получится
 * пять формулировок одного совета.
 */

/**
 * Первое, что стоит попробовать почти при любом сбое.
 *
 * `/start` перевыдаёт кнопку приложения со свежим адресом и кодом входа и
 * заново присылает меню. Этим лечится большинство настоящих отказов: устаревшая
 * кнопка из старого сообщения, истёкший вход, приложение, поднятое из кеша
 * клиента. Совет идёт последней строкой — сначала человек читает, что именно
 * случилось, и только потом что делать.
 */
export const RESTART_HINT =
  "Часто помогает /start в чате бота: он обновит кнопку приложения и вход.";
