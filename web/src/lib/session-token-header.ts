/**
 * Имя заголовка, которым сервер возвращает продлённый токен сессии.
 *
 * Лежит отдельным модулем, потому что нужен обеим сторонам: сервер ставит
 * заголовок, браузер его читает, а серверный `telegram-auth` тянет за собой
 * `node:crypto` и в клиентский бандл попадать не должен.
 */
export const SESSION_TOKEN_HEADER = "X-Session-Token";
